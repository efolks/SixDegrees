import axios from 'axios';

const initialState = {};

//Action Types
const GET_ACTOR_CREDITS = 'GET_ACTOR_CREDITS'

//Action Creators
//const getActorCredits = ()

//Thunks
//export const fetchActorCredits = ()

export default function(state = initialState, action) {
    switch (action.type) {
      default:
        return state
    }
  }
